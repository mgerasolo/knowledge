# Handoff — 2026-07-12 (Knowledge Capture + Ingestion)

> Written while you're away. Everything below is **reviewable and reversible** — I did **not** commit
> your untracked code, redeploy any service, or make outward-facing changes.

## TL;DR — what's confirmed
- ✅ **OpenAI quota is restored.** Your $15 fixed it — I verified with a billed test call on Speakr's
  transcription key (HTTP 200). The org-level `insufficient_quota` is gone.
- ✅ **iOS voice capture works and saves to the server.** Nothing you recorded is lost.
- ✅ The "File Too Large" errors were a **mislabel** of the OpenAI quota error — not size, not format,
  not your phone.

## What you need to do (2 minutes)
1. In Speakr (**https://transcripts.nextlevelguild.com/** — the root URL, NOT `/lecture`), tap **retry ↻**
   on a failed recording. It should transcribe now. (Or tell me and I'll re-queue it.)
2. Turn on **auto-recharge** on the NextLevelFoundry OpenAI org (Settings → Billing). A silent
   quota-out is what killed the *video* ingestion pipeline in May too — worth never hitting again.

## Decisions still needed from you
1. **Capture path** — after transcription works, do the one lock-screen test (record → lock phone 30–60s →
   stop → did audio survive?). If it survives → use Speakr's in-app recorder (zero build). If not →
   Voice Memos → upload. Spec: `docs/superpowers/specs/2026-07-12-knowledge-capture-design.md` §5/§9.
2. **Transcription wiring** — keep Speakr's direct-OpenAI (works now) vs reroute via LiteLLM proxy for
   cost monitoring (your earlier preference). Not urgent now that credit is back.
3. **Commit scope** — you asked to review untracked files first. The meaningful app code is ~22 py files
   under `src/` + `docker-compose.yml` + `deploy/` + `scripts/`; the rest is BMAD/Cursor scaffolding +
   planning docs. Say the word and I'll commit app-code-only (or all).
4. **Manifesto framing** — review `docs/PROJECT_MANIFESTO.md` (now includes KnowledgeCapture as product #6).

## What I did this session (all uncommitted, for your review)
| File | Change |
|------|--------|
| `docs/PROJECT_MANIFESTO.md` | Full project manifesto (PM/PMO, non-technical) + added **KnowledgeCapture** product line |
| `docs/superpowers/specs/2026-07-12-knowledge-capture-design.md` | **DRAFT** design spec for the raw-capture step (pending your approval) |
| `src/transcript-service/app.py` | **Cold-start fix** — worker now starts at **boot**, not on first request; `/health` returns 503 if the worker is dead |
| `src/transcript-service/backfill_worker.py` | Idempotent start + heartbeat + `worker_status()` for real liveness |
| `docs/HANDOFF-2026-07-12.md` | This file |

Both edited Python files **compile clean**. **Not deployed** — review the diff, then to ship the ingestion
fix: rebuild + redeploy `knowledge-transcript-service` on Banner (I can do it on your say-so). This is the
fix for the video pipeline going dormant on 2026-05-01.

## Two open threads (recap)
- **Thread A — Knowledge Capture (new):** capture Matt's raw voice ideas → refine into young-audience
  mindset courses. Design drafted, capture-path decision evidence-gated on the lock-screen test.
- **Thread B — Ingestion hardening (original):** video pipeline dormant since May 1 (cold-start bug).
  Fix implemented in source, awaiting your review + deploy. (The earlier background agent was stopped
  because its isolated worktree couldn't see the untracked code.)

## Bugs found (sidebars — worth GitHub issues)
- Speakr mislabels OpenAI `429 insufficient_quota` as **"File Too Large / enable chunking."** Very misleading.
- Speakr under the **`/lecture`** sub-path is broken (root-absolute asset/API paths escape the Traefik
  prefix → untranslated i18n keys + failed API calls). Use the root domain, or give Speakr its own
  subdomain / configure a base path.
- `knowledge.nextlevelguild.com` does not resolve (only `nextlevelfoundry.com` + `transcripts.nextlevelguild.com`).

## Where things live
- This conversation's worktree: `wt/status-manifesto` (branch). Docs above were written into the **main
  checkout** so they're visible in your Cursor window.
